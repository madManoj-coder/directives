import { ToLowercaseByInputDirective } from './to-lowercase-by-input.directive';

describe('ToLowercaseByInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ToLowercaseByInputDirective();
    expect(directive).toBeTruthy();
  });
});
